[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    e2e: marks end-to-end tests
    integration: marks integration tests
    unit: marks unit tests
    case: marks Case Management tests
